## Agentic SFT TunningFactory — 个人答疑 QA

> 本文档针对使用本框架过程中可能遇到的实际问题，提供详细的排查思路和解决方案。

---

### Q1: 我应该选择哪种微调方式？

**决策矩阵**：

| 场景 | 推荐方式 | 理由 |
|------|---------|------|
| GPU 资源充足，追求最佳效果 | Full SFT | 全参数更新，效果上限最高 |
| GPU 资源有限（单卡 24GB） | LoRA | 只训练 ~0.1% 参数，显存友好 |
| GPU 资源极度有限（单卡 16GB） | QLoRA (4-bit) | 模型量化 + LoRA，极致省显存 |
| 多模态模型微调 | Freeze + LoRA | 冻结视觉塔，LoRA 微调语言模型 |
| 快速实验/原型验证 | LoRA (rank=8) | 训练快，效果够用 |
| 生产级部署 | Full SFT 或 LoRA 合并 | 合并后无额外推理开销 |

**LoRA rank 选择**：
- rank=8：快速实验，参数量最少
- rank=16~32：生产级微调，效果与全参数接近
- rank=64~128：复杂任务，接近全参数效果

---

### Q2: 我的数据应该用 Alpaca 格式还是 ShareGPT 格式？

**Alpaca 格式**适合：
- 单轮问答数据
- 有明确的 instruction/input/output 结构
- 数据量较小，手动标注

```json
{"instruction": "翻译", "input": "Hello", "output": "你好"}
```

**ShareGPT 格式**适合：
- 多轮对话数据
- 从对话系统导出的数据
- 包含 system prompt 的数据

```json
{"conversations": [
    {"from": "system", "value": "你是一个翻译助手"},
    {"from": "human", "value": "翻译 Hello"},
    {"from": "gpt", "value": "你好"}
]}
```

**如何选择**：
- 如果数据是多轮对话 → ShareGPT
- 如果数据是单轮指令 → Alpaca（更简单）
- 两种格式效果无本质差异，只是数据组织方式不同

---

### Q3: 如何准备高质量的 SFT 数据？

**数据质量原则**：

1. **多样性**：覆盖目标任务的各种场景和边界情况
2. **一致性**：回复风格、格式保持统一
3. **准确性**：回复内容正确无误
4. **适当长度**：回复不宜过短（信息不足）或过长（冗余）

**数据量建议**：
- 1K~5K 高质量样本：足以让模型学会特定任务的格式和风格
- 10K~50K 样本：通用能力提升
- 100K+ 样本：大规模能力注入

**数据清洗要点**：
- 去除重复样本
- 去除空回复或异常短的回复
- 检查 instruction 和 response 的对应关系
- 统一格式（如 JSON 编码、换行符等）

---

### Q4: Template 选错了会怎样？如何确认正确的 Template？

**错误 Template 的症状**：
- Loss 不下降或下降极慢
- 生成的文本格式混乱
- 模型无法正确停止生成（无限输出）
- 训练日志中的 example 输出看起来不正常

**确认正确 Template 的方法**：

1. **查看模型文档**：模型的 HuggingFace 页面通常会说明对话格式
2. **查看 `constants.py`**：搜索模型名称，查看注册的 template
3. **查看训练日志**：训练开始时会打印 example，检查 tokenize 后的格式是否正确

**常用模型的 Template 对照表**：

| 模型 | Template |
|------|----------|
| Qwen / Qwen1.5 / Qwen2 | `qwen` |
| LLaMA-2 | `llama2` |
| LLaMA-3 | `llama3` |
| ChatGLM3 | `chatglm3` |
| Baichuan2 | `baichuan2` |
| Mistral / Mixtral | `mistral` |
| DeepSeek | `deepseek` |
| InternLM2 | `intern2` |
| Yi | `yi` |
| Gemma | `gemma` |

**Base 模型**（非 Chat 版本）可以使用 `default`、`alpaca`、`vicuna` 等通用模板。

---

### Q5: 训练时 Loss 一直不下降怎么办？

**排查清单**：

1. **检查 Template**（最常见原因）：
   - 查看训练日志中的 `training example` 输出
   - 确认 `input_ids` 和 `labels` 的格式正确
   - 确认 labels 中 prompt 部分被正确 mask 为 -100

2. **检查数据格式**：
   - 确认 JSON 格式正确
   - 确认列名映射正确（`--prompt`、`--query`、`--response`）
   - 确认数据不为空

3. **检查学习率**：
   - Full SFT：1e-5 ~ 5e-5
   - LoRA：1e-4 ~ 5e-4
   - 过大会导致 loss 震荡，过小会导致 loss 不动

4. **检查数据量**：
   - 数据量太少（<100 条）可能导致 loss 波动大
   - 增加 `--num_train_epochs` 或增加数据量

5. **检查模型加载**：
   - 确认模型路径正确
   - 确认模型权重完整（没有下载中断）

---

### Q6: 如何判断模型是否过拟合？

**过拟合信号**：
- `train_loss` 持续下降，但 `eval_loss` 开始上升
- 模型在训练数据上表现很好，但在新数据上表现差
- 模型开始"背诵"训练数据中的内容

**预防过拟合**：

| 方法 | 参数 | 说明 |
|------|------|------|
| 减少训练轮数 | `--num_train_epochs` | 最直接的方法 |
| 增大 Dropout | `--lora_dropout 0.1` | LoRA 训练时有效 |
| 早停 | `--early_stopping_patience 3` | 连续 3 次评估不改善则停止 |
| 增加数据量 | 更多训练数据 | 根本解决方案 |
| 正则化 | `--weight_decay 0.01` | 权重衰减 |
| 使用验证集 | `--val_size 0.1` | 划分 10% 数据作为验证集 |

---

### Q7: 如何在训练中同时进行评估？

**方式一：Loss 评估**（推荐）

```bash
python src/train_bash.py \
    --do_train --do_eval \
    --val_size 0.1 \
    --eval_strategy steps \
    --eval_steps 100 \
    ...
```

**方式二：生成式评估**

```bash
python src/train_bash.py \
    --do_train --do_eval \
    --predict_with_generate \
    --val_size 0.1 \
    --eval_strategy steps \
    --eval_steps 500 \
    ...
```

生成式评估会计算 BLEU 和 ROUGE 指标，但速度较慢。

---

### Q8: LoRA 训练完成后如何部署？

**两种部署方式**：

**方式一：合并后部署**（推荐）

```bash
# 1. 合并 LoRA 权重到基础模型
python src/export_model.py \
    --model_name_or_path base_model_path \
    --adapter_name_or_path lora_checkpoint_path \
    --template qwen \
    --finetuning_type lora \
    --export_dir merged_model_path

# 2. 像普通模型一样部署
python src/api_demo.py \
    --model_name_or_path merged_model_path \
    --template qwen
```

优点：推理时无额外开销，兼容所有推理框架。

**方式二：动态加载 Adapter**

```python
from peft import PeftModel
from transformers import AutoModelForCausalLM

base_model = AutoModelForCausalLM.from_pretrained("base_model_path")
model = PeftModel.from_pretrained(base_model, "lora_checkpoint_path")
```

优点：可以动态切换不同的 LoRA adapter，适合多任务场景。

---

### Q9: 多卡训练时如何选择 DeepSpeed 还是 DDP？

**决策指南**：

| 场景 | 推荐方案 |
|------|---------|
| LoRA + 7B 模型 + 多卡 | DDP（无需 DeepSpeed） |
| Full + 7B 模型 + 4~8 卡 | DeepSpeed ZeRO-2 |
| Full + 13B 模型 + 8 卡 | DeepSpeed ZeRO-2 或 ZeRO-3 |
| Full + 70B 模型 | DeepSpeed ZeRO-3 |
| 任何模型 + 单卡 | 不需要分布式 |

**启动方式**：

```bash
# DDP（不使用 DeepSpeed）
torchrun --nproc_per_node 4 src/train_bash.py ...

# DeepSpeed ZeRO-2
torchrun --nproc_per_node 4 src/train_bash.py \
    --deepspeed scripts/ds_zero2.json ...

# DeepSpeed ZeRO-3
torchrun --nproc_per_node 4 src/train_bash.py \
    --deepspeed scripts/ds_zero3.json ...
```

---

### Q10: 如何使用 ODPS 表作为训练数据？

在阿里内部环境中，可以直接使用 ODPS 表作为数据源：

```bash
python src/train_bash.py \
    --file_name "odps://your_project/tables/your_table/ds=20240101" \
    --prompt instruction \
    --response output \
    --tables "odps://your_project/tables/your_table/ds=20240101" \
    ...
```

框架在 `data/loader.py` 中检测 `odps://` 前缀，自动使用 ODPS 数据加载器。同样支持 `ailake://` 前缀的 AiLake 数据源。

---

### Q11: 如何从断点恢复训练？

```bash
python src/train_bash.py \
    ... \
    --resume_from_checkpoint output/checkpoint-500
```

**注意事项**：
- DeepSpeed 恢复需要保持相同的 GPU 数量和 DeepSpeed 配置
- LoRA 恢复需要指定 `--adapter_name_or_path` 为 checkpoint 路径
- 恢复后会从断点的 step 继续训练，学习率调度也会恢复

---

### Q12: 如何进行多数据集混合训练？

**方式一：通过 dataset_info.json 注册多个数据集**

```bash
python src/train_bash.py \
    --dataset alpaca_gpt4_zh,identity,lima \
    --mix_strategy concat \
    ...
```

**混合策略**：
| 策略 | 参数值 | 说明 |
|------|--------|------|
| 拼接 | `concat` | 简单拼接所有数据集（推荐非流式模式） |
| 交错欠采样 | `interleave_under` | 按概率采样，以最小数据集为准 |
| 交错过采样 | `interleave_over` | 按概率采样，以最大数据集为准 |

**自定义采样概率**：

```bash
--interleave_probs "0.5,0.3,0.2"  # 三个数据集的采样概率
```

**动态采样**：`--dynamic_probs` 启用动态采样概率调整。

---

### Q13: 训练时显存不够怎么办？（按优先级排列）

**第一步：减小 batch size**
```bash
--per_device_train_batch_size 1 \
--gradient_accumulation_steps 16  # 保持有效 batch size 不变
```

**第二步：减小序列长度**
```bash
--cutoff_len 1024  # 从 2048 减到 1024
```

**第三步：使用 QLoRA**
```bash
--finetuning_type lora \
--quantization_bit 4
```

**第四步：使用 DeepSpeed ZeRO-3**
```bash
--deepspeed scripts/ds_zero3.json
```

**第五步：使用 CPU Offload**
```bash
--deepspeed scripts/ds_zero3_cpuoffload.json
```

**第六步：使用梯度检查点**（默认已启用）
```bash
# 确认没有禁用
# 不要加 --disable_gradient_checkpointing
```

---

### Q14: 如何微调多模态模型（如 Qwen-VL）？

**数据准备**：

```json
[
  {
    "instruction": "<image>描述这张图片的内容",
    "input": "",
    "output": "图片中展示了一座古老的城市建筑...",
    "image": ["path/to/image.jpg"]
  }
]
```

**训练命令**：

```bash
python src/train_bash.py \
    --stage sft \
    --model_name_or_path Qwen/Qwen2-VL-7B-Instruct \
    --template qwen2_vl \
    --do_train \
    --file_name data/mm_data.json \
    --prompt instruction \
    --response output \
    --image image \
    --image_folder /path/to/images \
    --finetuning_type freeze \
    --freeze_module_prefix visual \
    --lora_target all \
    --output_dir output/qwen2-vl-sft \
    --bf16
```

**关键参数**：
- `--image image`：指定图像列名
- `--image_folder`：图像文件的根目录
- `--freeze_module_prefix visual`：冻结视觉编码器
- 文本中使用 `<image>` 占位符标记图像位置

---

### Q15: 如何使用 Packing 提升训练效率？

**启用 Packing**：

```bash
python src/train_bash.py \
    --packing \
    --neat_packing \
    --cutoff_len 4096 \
    ...
```

**Packing 的效果**：
- 短样本（平均长度 << cutoff_len）时效果最明显
- 可以将 GPU 利用率从 30%~50% 提升到 80%~95%
- 训练速度提升 2~5 倍

**注意事项**：
- `--neat_packing` 必须配合 `--packing` 使用
- Neat Packing 需要 FlashAttention-2 或 SDPA 支持
- Packing 会改变有效 batch size（每个 batch 包含更多样本）
- 预训练阶段会自动启用 Packing

---

### Q16: 训练完成后如何快速测试模型效果？

**CLI 对话测试**：

```bash
python src/cli_demo.py \
    --model_name_or_path output/merged_model \
    --template qwen
```

**LoRA 模型直接测试**（无需合并）：

```bash
python src/cli_demo.py \
    --model_name_or_path base_model_path \
    --adapter_name_or_path output/lora_checkpoint \
    --template qwen \
    --finetuning_type lora
```

**API 服务测试**：

```bash
# 启动服务
python src/api_demo.py \
    --model_name_or_path output/merged_model \
    --template qwen

# 调用 API
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{"model": "test", "messages": [{"role": "user", "content": "你好"}]}'
```

---

### Q17: 如何理解训练日志中的各项指标？

**训练日志示例**：
```
{'loss': 1.2345, 'learning_rate': 4.5e-05, 'epoch': 0.5}
```

| 指标 | 含义 | 正常范围 |
|------|------|---------|
| `loss` | 训练损失 | 应持续下降，最终收敛到 0.5~2.0 |
| `eval_loss` | 验证损失 | 应与 train_loss 趋势一致 |
| `learning_rate` | 当前学习率 | 按调度器变化 |
| `epoch` | 当前训练进度 | 0 到 num_train_epochs |
| `train_runtime` | 训练总时间 | - |
| `train_samples_per_second` | 每秒处理样本数 | 越大越好 |

**Loss 异常情况**：
- Loss 突然飙升 → 学习率过大或数据异常
- Loss 不下降 → Template 错误或学习率过小
- Loss 为 NaN → FP16 溢出，切换 BF16
- Loss 震荡剧烈 → 减小学习率或增大 batch size

---

### Q18: 项目中的 Turbo 模式是什么？

Turbo 模式通过 `--use_turbo` 参数启用，使用 `transformers_turbo` 库加速训练。

**加速原理**：
- 优化的 Attention 实现
- 融合的前向/反向传播算子
- 优化的数据加载流水线

**使用方式**：
```bash
pip install transformers_turbo
python src/train_bash.py \
    --use_turbo \
    --stage sft \
    ...
```

**注意**：Turbo 模式下 labels 的处理方式略有不同（shift 操作在 Collator 中完成），对应的 workflow 文件为 `workflow_turbo.py`。

---

### Q19: 如何自定义新的 Prompt Template？

在 `src/llmtuner/data/template.py` 中添加新的 Template 注册：

```python
# 在文件末尾添加
_register_template(
    name="my_model",
    format_user=StringFormatter(slots=["<|user|>\n{{content}}<|end|>\n"]),
    format_assistant=StringFormatter(slots=["<|assistant|>\n{{content}}<|end|>\n"]),
    format_system=StringFormatter(slots=["<|system|>\n{{content}}<|end|>\n"]),
    format_separator=EmptyFormatter(slots=[]),
    format_prefix=EmptyFormatter(slots=[{"bos_token"}]),
    default_system="You are a helpful assistant.",
    stop_words=["<|end|>"],
    efficient_eos=True,
)
```

然后使用 `--template my_model` 即可。

---

### Q20: 如何监控训练进度和实验管理？

**本地监控**：
- `--plot_loss`：训练结束后在 output_dir 生成 loss 曲线图
- `trainer_log.jsonl`：每个 logging step 的详细指标

**实验追踪平台**：

```bash
# 使用 ML Tracker（阿里内部）
--report_to ml_tracker

# 使用 SwanLab
--report_to swanlab

# 使用 TensorBoard
--report_to tensorboard
--logging_dir output/logs

# 使用 WandB
--report_to wandb
```

**MOS 模型管理**：
- 训练产出的 checkpoint 通过 `PushToMosCallback` 自动上传到 MOS
- 在 `scripts/*.sh` 中通过 `SAVE_MODEL` 变量指定 MOS 存储路径
- 格式：`your_project.your_model/version=your_version`
