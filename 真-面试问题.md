### 面试被问到的问题
- RL 和 Agentic RL 之间的区别
- 如何管理 RL 的长上下文，训练时长轨迹如何高效的梯度更新，上下文窗口装不下怎么办，截断、压缩、还是改训练范式？
- chat—template 作用？原理？

#### 2025.4.1
蚂蚁 - 一面
【转正实习】大模型智能体优化算法 
CTO-*-Vanguard

- 简历
意图体系建立，目的？
怎么定义高质量数据的？
reward搭建的过程？
最后是如何验证轨迹是否是好轨迹的？
最终怎么评价项目成果指标的？
sft的label是什么？
自己做过sft吗，遇到过什么困难？
分布式训练逻辑？困难？

- 知识
训练时各种模型的参数分别是什么意思？
chat-template作用？原理？
一条输入在语言模型中经历了什么到输出，完整具体流程？

- 漫谈
你关注的AI热点是什么？
你用过claudecode做什么项目，怎么做的，流程？

- 不足：
模型细节理解不是很深
训练框架里的细节不是很清楚


#### 2025.4.2
阿里云-PAI 一面 挂
算法工程师-强化学习
阿里云-阿里云技术线-PAI
- 询问你在高德实习期间负责的用户意图识别项目，具体承担了哪些工作。
- 追问该项目的数据基础存在哪些典型问题，以及你采取了哪些针对性解决措施。
- 询问为什么选择用 Reward Prompt 替代传统 Reward Model，这一设计的出发点和实现逻辑是什么。
- 追问 Reward Prompt 的设计难点，以及你如何验证其打分结果的可靠性。
- 询问在 Reward Prompt 迭代过程中，是否因为 Prompt 设计缺陷导致过资源浪费，并要求说明具体迭代过程。

- 询问项目迭代中，是否频繁出现因 Prompt 设计或模型行为偏差导致返工的问题，以及这类问题如何系统性解决。
- 追问整个项目的分工结构，尤其是 RL 与数据构建部分，是由你独立承担还是团队协作完成。
- 询问当前模型是否已经在高德 App 正式上线，以及项目更偏研究探索还是已承载实际业务流量。
- 追问除具体业务场景外，该项目在 RL 方法论层面是否有可迁移的通用创新。
- 询问为什么CPT基座模型选择自研 8B Base Model，而不是直接适配更大的开源模型，并追问是否评估过 MOE 架构。
- 询问模型在线上运行时，是纯生成式 Base Model，还是与工具调用系统深度集成，以及具体调用逻辑是什么。
- 追问你在百度实习期间参与的刷单识别项目中，基于大模型 SFT 的召回率为何只有 94%，以及漏掉的 6% 样本具有什么特征。
- 询问在高德半年实习期间，你认为最大的收获和对个人成长影响最深的部分是什么。
- 追问实验室算力资源是否限制了训练效率，以及在高德使用 VVR 框架训练时，是否对速度瓶颈做过分析或优化。

- 询问你在模型训练中最常用的 loss 是哪一种，是否使用过多种 loss，以及 baseline loss 的原始计算方式。
- 询问你日常主要使用哪些 AI 编程辅助工具，是否使用过阿里云相关的 AI Coding 工具。
- 询问你所在团队当前的技术方向和主要工作内容，以及是否仍在开展大规模 Base Model 训练。

提问：
- 追问团队对外提供的技术能力定位，以及与通义千问等大模型之间的协作关系。
- 追问团队在 IL 训练优化中重点关注哪些问题，以及与客户在效果优化、速度优化上的分工方式。
- 询问你所从事的 AI Infra 优化工作属于哪个技术层级，是否涉及操作系统或硬件驱动层。

---

# 📝 面试问题回答

---

## 一、通用知识类问题

### Q: RL 和 Agentic RL 之间的区别

**回答：**

| 维度 | 传统 RL（如 RLVR） | Agentic RL |
|------|-------------------|-----------|
| **交互方式** | 单轮：prompt → response，一次性生成完整回答 | 多轮：prompt → action → observation → action → ...，与外部环境持续交互 |
| **环境** | 无外部环境，直接验证答案正确性（如数学答案匹配、代码执行） | 有真实/模拟环境（如MCP工具Server、WebShop、游戏等），需要与环境交互获取observation |
| **Rollout** | 一次性生成（GenerateScheduler），模型输出完整response后计算reward | 多轮交互（RolloutScheduler），每轮生成action → 调用环境 → 获取observation → 继续生成 |
| **奖励信号** | 最终奖励（对/错的二值reward），信号清晰 | 可以是step-level或trajectory-level，信号稀疏且延迟 |
| **状态空间** | 相对简单（输入prompt + 生成的response） | 极其复杂（对话历史 + 工具返回结果 + Context状态），维度高且稀疏 |
| **核心挑战** | 奖励设计、探索效率 | Echo Trap（回声陷阱）、Void Turn（空转）、长轨迹上下文管理、工具I/O瓶颈 |
| **典型算法** | PPO、GRPO、Reinforce++ | StarPO（trajectory-level）、GiGPO（step-level）、DA-GRPO |
| **训练范式** | TrajectoryWise | TrajectoryWise 或 StepWise |

**结合我的项目经验：**
在STAgent项目中，我们做的就是Agentic RL——模型需要在MCP工具环境中进行多轮交互（最多4轮工具调用），每轮生成Thought→Action，然后真实调用工具获取Observation。这带来了三个传统RL没有的挑战：
1. **Echo Trap**：模型在thinking中陷入重复循环，永远不推进到工具调用
2. **Void Turn**：模型产生空转，既不调用工具也不给出答案
3. **Reward方差坍缩**：同一query的所有rollout全部成功或全部失败，GRPO学不到东西

我们通过DA-GRPO算法（融合SFT先验权重和RL方差权重的difficulty-aware加权）解决了这些问题。

---

### Q: 如何管理 RL 的长上下文，训练时长轨迹如何高效的梯度更新，上下文窗口装不下怎么办？

**回答：**

这是Agentic RL的核心挑战之一。我们在STAgent项目中从**数据层、模型层、训练层**三个维度解决：

**1. 数据层——压缩上下文**
- **Context机制**：这是最核心的设计。工具的完整返回结果存储在Server端的ContextManager中，LLM上下文中只保留精简的ID引用（如`"poi_abc123"`），而非完整的POI数据。后续工具通过ID引用直接获取完整数据。这极大压缩了上下文长度。
- **控制工具调用轮数**：限制最多4轮工具调用，过滤掉超长轨迹的训练数据
- **Thinking长度约束**：限制在≤200 tokens，避免冗余思考

**2. 模型层——扩展窗口能力**
- 使用Qwen3-30B-A3B，原生支持较长的上下文窗口
- 在ROLL框架中配置`sequence_length=40960`，覆盖多轮交互轨迹
- 使用SGLang的RadixAttention复用同一query的16个rollout之间共享的prefix KV cache

**3. 训练层——高效梯度更新**
- **截断策略**：`max_tokens_per_step=4096`（每轮交互最多4096 token）+ `max_actions_per_traj=15`（每条轨迹最多15轮），超出则强制截断
- **Gradient Checkpointing**：用计算换显存，降低训练显存峰值，使长序列可以放入GPU
- **梯度裁剪**：`max_grad_norm=1.0`，多轮交互梯度波动大，需要裁剪防止梯度爆炸
- **序列并行**：在SFT阶段使用Ulysses/Zigzag-Ring序列并行，将超长序列切分到多个GPU上并行处理

**如果上下文窗口真的装不下：**
- **优先压缩**：通过Context机制压缩中间结果，这是最有效的方式
- **其次截断**：设置合理的max_tokens和max_actions限制，超出则强制进入最终回答阶段
- **最后改训练范式**：如果任务确实需要超长轨迹，可以考虑StepWise训练（如GiGPO），对每个step单独计算advantage，而非整条轨迹。这样每次梯度更新只需要当前step的上下文，不需要完整轨迹

---

### Q: chat-template 作用？原理？

**回答：**

**作用：**
chat-template的核心作用是**将多轮对话格式化为模型可理解的token序列**。它定义了如何将system prompt、user message、assistant response等不同角色的内容，用特殊token标记拼接成一个完整的输入序列。

**为什么需要它：**
- LLM本质上只接受一维的token序列作为输入，但对话是结构化的（有角色、有轮次）
- 不同模型使用不同的特殊token来标记角色边界（如Qwen用`<|im_start|>`/`<|im_end|>`，LLaMA用`[INST]`/`[/INST]`）
- 如果template选错，模型无法正确识别"哪部分是用户输入、哪部分是需要生成的回复"，导致loss不下降、生成格式混乱、无法正确停止生成

**原理（以Qwen为例）：**

一条多轮对话经过chat-template处理后变成：
```
<|im_start|>system
你是一个时空规划助手<|im_end|>
<|im_start|>user
帮我找附近的餐厅<|im_end|>
<|im_start|>assistant
好的，我来帮你搜索...<|im_end|>
```

在**SFT训练**中，template还决定了**Label Masking**的边界：
```
input_ids:  [BOS] [system tokens] [user tokens] [assistant tokens] [EOS]
labels:     [-100] [-100]         [-100]         [assistant tokens] [EOS]
```
- prompt部分（system + user）的labels设为`-100`（PyTorch CrossEntropyLoss自动忽略）
- 只在assistant response部分计算loss，让模型学习"如何回答"而非"背诵问题"

在**RL训练**中，template用于将原始数据转换为模型输入格式（tokenize阶段），确保rollout生成的格式与SFT阶段一致。

**在我们项目中的实践：**
- SFT框架（LLaMA-Factory二次开发）中，template在`data/processors/supervised.py`的`_encode_supervised_example()`中实现
- ROLL框架中，通过`data_args.template: qwen2_5`配置，自动应用对应模型的chat-template
- 选错template是常见的训练bug——症状是loss不下降或生成格式混乱，排查方法是检查训练日志中打印的tokenized example

---

## 二、蚂蚁一面问题回答（2025.4.1）

### 简历相关

#### Q: 意图体系建立，目的？

**回答：**
意图体系的目的是**让模型准确理解用户的出行意图，从而正确选择和调用地图工具**。

具体来说，高德智能助手接收的用户query千差万别（"附近有什么好吃的"、"帮我规划三天两夜的旅行"、"先去加油再导航回家"），模型需要先理解用户意图，才能决定调用哪些工具、传什么参数。

我们建立的意图体系包含**5个决策阶段、18种意图类型、32个主叶子节点和41个辅助叶子节点**。它的作用是：
1. **数据筛选**：从428万条线上日志中，通过7阶段Pipeline筛选出符合时空规划场景的高质量query
2. **数据标注**：为每条query打上意图标签，用于SFT数据构造和评测
3. **难度分层**：不同意图类型对应不同的工具调用复杂度，用于DA-GRPO的difficulty-aware训练
4. **评测覆盖**：确保评测集覆盖所有意图类别，避免能力盲区

"自进化"体现在：体系从v2.0迭代到v2.1，每次升级都是基于Pipeline输出的分布分析——当发现大量query无法被现有类目覆盖时，由LLM辅助分析共性特征并提议新增类目（如v2.1新增了"应用交互"决策阶段和"along_route"空间约束）。

---

#### Q: 怎么定义高质量数据的？

**回答：**
我们通过**三级漏斗过滤 + LLM-as-a-Judge多维评分**来定义高质量数据：

**三级漏斗：**
1. **规则过滤**（428万→~240万）：过滤ASR乱码、超短query、纯表情/特殊字符
2. **意图过滤**（240万→~24万）：由LLM判断是否属于时空规划范畴，过滤闲聊、系统指令等
3. **质量过滤**（24万→~10万）：过滤意图模糊、信息不完整、复杂度过低的query

**质量评分标准（6维度）：**
- **需求理解**：是否正确解析了用户意图
- **工具调用**：工具选择是否正确、参数是否准确
- **幻觉控制**：是否存在编造信息
- **信息组织**：回答结构是否清晰
- **冲突解决**：多工具返回结果矛盾时的处理能力
- **协议遵循**：是否遵循输出格式规范

每条query生成8个answer，由Qwen3-235B作为Judge模型打分（0-4分），保留评分≥3分的top-2，最终得到约2.6万条SFT数据。数据分为Strong（高质量标杆）、Weak（一般质量）、Reject（负样本）三个层级。

---

#### Q: reward搭建的过程？

**回答：**
Reward搭建经历了**从简单到复杂的迭代过程**：

**第一版：简单的工具调用reward**
- 调用工具给负reward（惩罚冗余调用），调用成功给正向reward
- 问题：负reward设置过于极端，限制了模型的工具调用，模型变得"不敢调工具"

**第二版：指数增长的负reward**
- 工具调用给指数增长的负reward，不做硬截断（软截断）
- 改善了第一版的问题，但reward维度太单一

**第三版（最终版）：三维动态加权Reward**
- **推理（Reasoning）**：评估工具选择是否正确、调用顺序是否合理、参数传递是否准确。Rule-based检查 + LLM-as-a-Judge评分
- **忠诚度（Loyalty）**：双重含义——对用户指令的遵循度 + 对客观地理事实的忠实度。自动对比模型输出与工具返回的ground truth，检测幻觉
- **呈现（Presentation）**：评估最终回答的表达质量、格式规范性。主要依赖LLM-as-a-Judge

**配套机制：**
- 幻觉一票否决：如果模型编造不存在的地点/价格/时间，直接判定为Reject
- 参数精确性检查：检查工具调用参数是否正确
- 拒绝回答惩罚：模型不应该拒绝合理的用户请求

**为什么选Rule-based + LLM-as-Judge而非训练Reward Model：**
1. 工具调用的正确性可以用规则精确判断，准确率接近100%
2. 训练RM需要大量人工偏好标注数据，成本高
3. Rule-based可解释性强，方便debug
4. 修改规则立即生效，迭代速度快

---

#### Q: 最后是如何验证轨迹是否是好轨迹的？

**回答：**
我们通过**多层次验证机制**来判断轨迹质量：

1. **Rule-based自动验证**：
   - 工具调用格式是否正确（JSON Schema校验）
   - 必填参数是否齐全
   - 工具返回结果是否被正确使用（对比模型输出与工具返回的ground truth）
   - 是否存在幻觉（编造不存在的POI、错误的距离/方向）

2. **LLM-as-a-Judge评分**：
   - 使用Qwen3-235B作为Judge模型，采用MTBench的Reference-guided评分方法
   - 6维度评分（需求理解、工具调用、幻觉控制、信息组织、冲突解决、协议遵循）
   - 支持三种评估模式：Tier分桶（Strong/Weak/Reject）、Pointwise评分、Pairwise对比

3. **DA-GRPO的在线验证**：
   - 在RL训练中，对每个query生成一组rollout，计算reward标准差
   - 高方差样本（模型"有时对有时错"）是最有学习价值的
   - 方差为0的样本（全部成功或全部失败）直接过滤

4. **人工评测**：
   - GSB（Good/Same/Bad）白盒评测（产品+算法团队）
   - 黑盒评测（产品团队独立评估，算法团队不可见）

---

#### Q: 最终怎么评价项目成果指标的？

**回答：**
项目成果通过**自动化评测 + 人工评测**双重指标评价：

**自动化指标：**
- **TravelBench评分**：70.3分，超越671B的DeepSeek-R1和235B的Qwen3
- **工具使用率**：从50%提升到98.7%（引入三阶段状态机后）
- **Echo Trap发生率**：从~30%降到<2%（DA-GRPO的效果）
- **参数效率**：30B MoE模型（激活3B参数），参数效率提升200倍以上

**人工评测指标：**
- 每个场景（路线规划、工具指令、自主出行等8个场景）300-500条Pairwise偏好数据
- GSB评测：对比有/无状态机约束、有/无DA-GRPO的模型输出

**工程指标：**
- 首Token时延控制在8秒以内
- 工具调用≤4次

---

#### Q: SFT的label是什么？

**回答：**
SFT的label就是**assistant response部分的token IDs**。

具体来说，在SFT训练中，一条对话数据经过chat-template处理后：
```
input_ids:  [BOS] [system tokens] [user tokens] [assistant tokens] [EOS]
labels:     [-100] [-100]         [-100]         [assistant tokens] [EOS]
```

- **Prompt部分**（system + user）的labels设为`-100`（IGNORE_INDEX），PyTorch的CrossEntropyLoss自动忽略这些位置，不计算梯度
- **Response部分**（assistant）保留真实的token ID作为labels
- 模型的训练目标是：给定prompt，学会生成正确的response（Next Token Prediction）

在我们的项目中，SFT的label来源是：
- **Teacher模型生成**：使用Qwen-plus-latest/Qwen3-235B作为teacher模型，对每条query生成多个answer
- **LLM-as-a-Judge筛选**：保留评分最高的answer作为最终label
- **人工校验**：对关键场景的label进行人工审核

---

#### Q: 自己做过SFT吗，遇到过什么困难？

**回答：**
做过，我在高德实习期间基于LLaMA-Factory二次开发的框架做SFT训练。遇到的主要困难：

**1. Template选择问题**
- 早期选错了chat-template，导致loss不下降、生成格式混乱
- 解决：检查训练日志中打印的tokenized example，确认token化后的格式是否正确

**2. 数据质量问题**
- Teacher模型生成的轨迹中存在幻觉（编造不存在的POI）、工具调用参数错误等问题
- 解决：设计了5种错误类型的Verifier（E001-E005）进行自动检测，结合LLM-as-a-Judge多维评分筛选

**3. 工具使用率低**
- SFT后模型的工具使用率只有约50%，很多时候模型直接"编"答案而不调用工具
- 解决：设计了三阶段状态机（寻找事实→冲突处理→最终审计），在SFT数据中通过特殊token标记状态转换，强制模型先规划工具调用链。工具使用率提升到98.7%

**4. 多任务数据配比**
- 垂域数据（12K）和通用数据（38K）的配比影响模型的通用能力和垂域能力平衡
- 解决：最终确定25%垂域 + 75%通用的配比，通过DynamicInterleaveCallback根据eval loss动态调整采样概率

---

#### Q: 分布式训练逻辑？困难？

**回答：**

**SFT阶段的分布式训练：**
- 使用DeepSpeed ZeRO-2/ZeRO-3进行数据并行训练
- ZeRO-2：将优化器状态和梯度分片到不同GPU，每卡仍持有完整模型参数
- ZeRO-3：将优化器状态、梯度、模型参数全部分片，显存节省最大
- 配合`overlap_comm=true`（通信与计算重叠）、`reduce_scatter=true`（减少通信量）优化

**RL阶段的分布式训练（ROLL框架）：**
- 基于Ray的多角色分布式架构：Actor Train（训练）、Actor Infer（推理）、Reference（参考模型）各自是独立的Cluster
- 支持Colocated部署（训练和推理共享GPU，通过offload/reload时分复用）和Disaggregated部署（使用不同GPU）
- Megatron 5D并行：TP（张量并行）+ PP（流水线并行）+ DP（数据并行）+ EP（专家并行）+ CP（上下文并行）

**CPT阶段的分布式训练：**
- 使用Open-Megatron-LLaMA框架，64 GPU（H200/H20）
- 纯数据并行（TP=1, PP=1, DP=64），因为Qwen3-1.7B单卡放得下

**遇到的困难：**
1. **GPU显存争抢**：RL训练中Actor训练、Rollout推理、Ref模型log_prob三者共享GPU显存。解决：调整`gpu_memory_utilization`（SGLang占70%-80%）+ gradient_checkpointing + ref param_offload
2. **多节点通信问题**：多节点启动后卡住不动，RANK≠0的节点找不到调度文件。解决：检查OSS挂载路径是否可写，设置正确的NCCL网络接口
3. **Megatron权重转换**：Qwen3在HF↔Megatron格式转换时，embedding权重共享（untie_embeddings_and_output_weights）导致转换失败。解决：手动调试转换脚本，处理词表大小（VOCAB_SIZE=151936）和HuggingFace Tokenizer集成
4. **长尾等待**：异步rollout中最慢的轨迹决定整个batch的完成时间。解决：设置`async_generation_ratio=4`异步生成 + LRU缓存减少工具I/O

---

### 知识相关

#### Q: 训练时各种模型的参数分别是什么意思？

**回答：**

**学习率（learning_rate）：**
- 控制每次参数更新的步长。SFT通常1e-5~5e-5，RL通常1e-6~1e-7（比SFT低1-2个数量级，防止策略崩溃）
- CPT中使用基座预训练学习率的1/10~1/5

**Batch Size相关：**
- **per_device_train_batch_size**：每个GPU每次前向传播处理的样本数
- **gradient_accumulation_steps**：梯度累积步数，等效增大batch size而不增加显存
- **global_batch_size** = per_device × gradient_accumulation × dp_size
- 在RL中还有**rollout_batch_size**（每步rollout的prompt数量）和**num_return_sequences_in_group**（每个prompt生成几个response）

**温度（temperature）：**
- 控制采样的随机性。训练时用高温度（0.99）鼓励探索，验证时用低温度（0.6）追求最佳表现
- temperature=0时退化为贪心解码

**Top-p / Top-k：**
- Top-p（nucleus sampling）：只从累积概率≥p的token中采样
- Top-k：只从概率最高的k个token中采样

**KL系数（use_kl_loss）：**
- 控制RL策略偏离SFT初始化的程度。我们设为0.001，较小的值允许更多探索

**max_grad_norm：**
- 梯度裁剪阈值，防止梯度爆炸。Agentic场景设为1.0

**warmup_steps/warmup_ratio：**
- 学习率预热步数/比例。训练初期从0线性增长到目标学习率，防止初期不稳定

**weight_decay：**
- 权重衰减，L2正则化，防止过拟合

**LoRA相关：**
- **lora_rank (r)**：低秩分解的秩，越大表达能力越强但参数越多。常用8/16/32
- **lora_alpha (α)**：缩放因子，实际增量为`(α/r) × BA`，默认α=2r

---

#### Q: chat-template作用？原理？

（已在通用知识类问题中详细回答，此处不再重复）

---

#### Q: 一条输入在语言模型中经历了什么到输出，完整具体流程？

**回答：**

**1. 文本预处理**
- 用户输入的原始文本经过chat-template格式化，加上特殊token标记角色边界
- 例：`<|im_start|>user\n帮我找附近的餐厅<|im_end|>\n<|im_start|>assistant\n`

**2. Tokenization**
- Tokenizer将文本切分为token序列，每个token映射为一个整数ID
- 例：`"帮我找附近的餐厅"` → `[5765, 2345, 8901, ...]`
- 同时生成attention_mask（标记哪些位置是有效token，哪些是padding）

**3. Embedding层**
- 将token ID通过Embedding矩阵（词表大小×隐藏维度，如151936×2048）查表得到向量表示
- 加上位置编码（Qwen使用RoPE旋转位置编码），让模型感知token的位置信息

**4. Transformer层（重复N层）**
每一层包含：
- **Multi-Head Self-Attention**：
  - 将输入投影为Q、K、V三个矩阵
  - 计算注意力分数：`Attention(Q,K,V) = softmax(QK^T / √d_k) × V`
  - 使用causal mask确保每个token只能看到它之前的token（自回归）
  - 多头注意力将隐藏维度拆分为多个头，并行计算后拼接
- **Feed-Forward Network (FFN)**：
  - 两层线性变换 + 激活函数（如SwiGLU）
  - 扩展维度后再压缩回来，增加模型的非线性表达能力
- 每个子层都有**残差连接**和**LayerNorm/RMSNorm**

**5. 输出层**
- 最后一层Transformer的输出经过一个线性层（隐藏维度→词表大小），得到logits
- logits表示词表中每个token的未归一化概率

**6. 采样/解码**
- 对logits应用temperature缩放：`logits = logits / temperature`
- 应用Top-p/Top-k过滤
- 通过softmax转为概率分布
- 从分布中采样得到下一个token
- 将生成的token拼接到输入序列末尾，重复步骤3-6，直到生成EOS token或达到最大长度

**7. 后处理**
- Tokenizer将生成的token ID序列解码回文本
- 去除特殊token，返回最终的文本回答

---

### 漫谈

#### Q: 你关注的AI热点是什么？

**回答：**
我主要关注以下几个方向：

1. **Agentic AI**：这是我实习工作的核心方向。关注RAGEN、SimpleTIR等Agentic RL的学术进展，以及MCP协议在工具调用标准化方面的发展。我认为Agent是LLM从"对话工具"走向"自主执行者"的关键一步。

2. **Reasoning模型**：DeepSeek-R1开创了用RL训练推理能力的范式，GRPO算法证明了不需要Critic网络也能做好RL。我在项目中也是基于GRPO进行扩展（DA-GRPO）。

3. **长上下文与高效推理**：SGLang的RadixAttention、vLLM的PagedAttention等推理优化技术，以及序列并行（Ulysses/Ring Attention）等训练优化技术。

4. **AI Coding**：Claude Code、Cursor等AI编程工具的发展。我自己也在用Claude Code做项目，体验了AI辅助编程的完整流程。

---

#### Q: 你用过Claude Code做什么项目，怎么做的，流程？

**回答：**
我用Claude Code主要做了面试准备相关的文档整理和技术总结工作：

1. **项目文档整理**：让Claude Code帮我梳理高德实习期间的技术文档，将分散的技术细节整合成结构化的面试准备材料
2. **技术深挖**：针对项目中的关键技术点（如DA-GRPO算法、Context机制、分布式训练），让Claude Code帮我从多个角度深入分析，准备面试官可能的追问
3. **代码理解**：让Claude Code帮我阅读和理解ROLL框架、LLaMA-Factory的源码，梳理核心模块的调用链

流程上：
- 先给Claude Code提供项目的背景信息和文档
- 然后提出具体的问题或任务
- Claude Code会自主阅读相关文件、搜索代码库、生成回答
- 我再根据实际情况进行修正和补充

---

## 三、阿里云PAI一面问题回答（2025.4.2）

### Q: 在高德实习期间负责的用户意图识别项目，具体承担了哪些工作？

**回答：**
我在高德AI团队实习约半年，主要参与了STAgent（时空规划Agent）项目，具体承担以下工作：

**1. 数据工程（核心负责）：**
- 搭建了7阶段意图分类Pipeline，从428万条线上日志中筛选出10万条高质量query
- 设计了从v2.0迭代到v2.1的自进化意图体系（5个决策阶段、18种意图类型、32+41个叶子节点）
- 用teacher模型生成轨迹，每条query生成8个answer，通过LLM-as-Judge打分后保留top-2，最终得到约2.6万条SFT数据

**2. SFT训练（参与设计）：**
- 参与设计三阶段状态机（寻找事实→冲突处理→最终审计），将工具使用率从50%提升到98.7%
- 设计Context机制，通过ID引用实现跨工具的数据传递

**3. RL训练（核心负责）：**
- 基于ROLL框架对SFT后的Qwen-30B-A3B进行RL训练
- 解决工具调用负惩罚导致模型保守的问题，从GRPO切换至GSPO
- 设计DA-GRPO算法（Difficulty-Aware GRPO），融合SFT先验权重和RL方差权重
- 设计三维动态加权Reward（推理/忠诚度/呈现）+ 幻觉一票否决机制
- 调整分配解决异步Rollout资源瓶颈

---

### Q: 该项目的数据基础存在哪些典型问题，以及采取了哪些针对性解决措施？

**回答：**

**问题1：原始数据噪声极大**
- 428万条线上日志中包含大量ASR识别错误导致的乱码、超短query、纯表情等无效数据
- 解决：三级漏斗过滤，总留存率约2.4%

**问题2：意图分布极度不均衡**
- "路线规划"类query占比超过60%，而"沿途搜索"、"多人出行"等长尾意图占比不到5%
- 解决：在意图体系中细化长尾类目，在数据采样时对低频意图进行上采样

**问题3：Teacher模型生成的轨迹存在幻觉**
- Teacher模型（Qwen-plus-latest）在生成工具调用轨迹时，会编造不存在的POI、错误的距离方向
- 解决：设计5种错误类型的Verifier进行自动检测 + LLM-as-Judge多维评分筛选 + 幻觉一票否决

**问题4：多意图query的处理**
- 用户经常发出复合意图（如"先去加油再导航回家"），需要分解为多个子意图
- 解决：Pipeline中的"意图分解"阶段，由LLM将复合意图拆分为独立的子意图

---

### Q: 为什么选择用 Reward Prompt 替代传统 Reward Model？

**回答：**
选择Reward Prompt（即Rule-based + LLM-as-a-Judge）而非训练Reward Model，出发点有四个：

1. **Agentic场景的特殊性**：工具调用的正确性是可以用规则精确判断的（参数是否正确、返回结果是否被正确使用），Rule-based在这些维度上的准确率接近100%，远高于RM。

2. **数据成本**：训练RM需要大量人工偏好标注数据。我们每个场景需要300-500条Pairwise偏好数据，8个场景共需2400-4000条，这个规模对于训练泛化能力强的RM是不够的。

3. **可解释性**：Rule-based的reward可以精确定位扣分原因（如"第2轮工具调用参数错误"），方便debug和迭代。RM给出的是标量分数，难以解释。

4. **迭代速度**：修改一条规则立即生效，而重新训练RM需要数小时到数天。在快速迭代的实习项目中，这个优势非常明显。

**实现逻辑：**
- Rule-based部分：自动检查工具调用格式、参数正确性、输出与工具返回结果的一致性
- LLM-as-Judge部分：使用Qwen3-235B对难以用规则覆盖的主观维度（回答自然度、用户体验）进行评分
- 两者融合为三维Reward（推理、忠诚度、呈现），动态加权

---

### Q: Reward Prompt 的设计难点，以及如何验证其打分结果的可靠性？

**回答：**

**设计难点：**
1. **维度定义的完备性**：需要确保三维Reward（推理、忠诚度、呈现）能覆盖所有质量维度，不遗漏关键缺陷
2. **评分一致性**：LLM-as-Judge的评分需要在不同query、不同场景下保持一致的标准
3. **硬约束与软约束的平衡**：幻觉是硬约束（一票否决），而信息组织是软约束（扣分但不否决），需要合理设计权重

**验证可靠性的方法：**
1. **人工标注对照**：随机抽取500条样本，人工标注质量等级（Strong/Weak/Reject），与LLM-as-Judge的评分进行一致性对比
2. **回归分析**：对工具对评分的影响做回归分析，发现`get_navigation`在所有维度上正向，`search_poi_along_route`在三个维度上显著负向——这与业务直觉一致，验证了评分的合理性
3. **消融实验**：分别去掉三个维度中的某一个，观察训练效果的变化，验证每个维度的贡献
4. **GSB评测**：用评分筛选出的数据训练的模型，通过人工GSB评测验证最终效果

---

### Q: Reward Prompt 迭代过程中，是否因为 Prompt 设计缺陷导致过资源浪费？

**回答：**
确实有过。最典型的一次是**RL版本4**：

在第4轮RL迭代中，我们将工具调用的负reward设置过于极端（指数增长的惩罚），导致模型学会了"不调用工具"来避免惩罚。这意味着之前花在rollout生成上的GPU时间（96 GPU × 数小时）全部浪费了，因为模型的行为完全偏离了预期。

**具体迭代过程：**
- **V1**：简单的工具调用±reward → 模型保守，不敢调工具
- **V2**：加入Reward Prompt v1 → 评分维度不够全面
- **V3**：换成30B-A3B模型 → OOM，需要调整并行策略（tp=cp=2, pp=ep=4）
- **V4**：负tool reward过于极端 → 模型完全不调工具，浪费大量资源
- **V5**：基于Think model + GSPO → 工具连接timeout
- **V6（最终版）**：三维动态加权Reward + DA-GRPO → 训练稳定，效果达标

**系统性解决方案：**
- 每次大改reward设计前，先在小数据+少步数上验证（小规模实验），确认有效后才上全量
- 建立reward监控看板，实时观察各维度的分数分布变化
- 设计了DA-GRPO的方差过滤机制，自动过滤无效样本

---

### Q: 整个项目的分工结构？

**回答：**
项目是团队协作完成的，大约4-5人的小团队：

- **我（实习生）**：核心负责数据工程（意图体系、Pipeline、数据筛选）和RL训练（DA-GRPO设计、Reward迭代、训练调优）
- **mentor（正式员工）**：负责整体技术方案设计、SFT训练框架搭建、状态机设计
- **另一位同学**：负责MCP工具Server的开发和维护、Context机制的工程实现
- **数据同学**：负责线上日志的采集和预处理
- **产品同学**：负责评测标准制定和人工评测

RL部分（DA-GRPO算法设计、Reward迭代、训练调优）主要由我独立承担，数据构建部分我负责Pipeline设计和执行，工程部分（MCP Server、Context机制）由团队协作完成。

---

### Q: 当前模型是否已经在高德 App 正式上线？

**回答：**
项目在我实习期间更偏**研究探索阶段**，但已经在内部进行了多轮评测和验证：

- 模型在TravelBench上拿到70.3分，超越了DeepSeek-R1和Qwen3-235B
- 通过了内部的GSB白盒评测和黑盒评测
- 工具调用通过FastMCP协议连接高德的真实API（POI搜索、导航、航班等）
- 但尚未承载实际业务流量，还在进行线上A/B测试的准备工作

项目的定位是为高德智能助手的下一代升级提供技术储备，验证了Agentic RL在时空规划场景的可行性。

---

### Q: 该项目在 RL 方法论层面是否有可迁移的通用创新？

**回答：**
有两个可迁移的通用创新：

**1. DA-GRPO算法**
- 核心思想：不是所有样本对训练的贡献相同，应该让模型把精力集中在"正在学习但还没学会"的样本上
- 通用性：任何使用GRPO的RL训练都可以引入difficulty-aware加权，不限于Agentic场景
- 公式：`W_merge(x) = W_sft(x) × (1 + λ × W_rl(x))`，融合SFT先验和RL在线信号

**2. 三阶段状态机格式**
- 核心思想：通过结构化的推理格式（寻找事实→冲突处理→最终审计）约束模型的推理过程
- 通用性：任何需要多步推理和工具调用的Agent场景都可以借鉴这种状态机设计
- 与ReAct的区别：ReAct是自由格式的Thought→Action→Observation循环，状态机则提供了更强的结构约束，防止Echo Trap

---

### Q: 为什么CPT基座模型选择自研 8B Base Model，而不是直接适配更大的开源模型？是否评估过 MOE 架构？

**回答：**

**选择Qwen3-1.7B（实际是1.7B而非8B）的原因：**
1. **资源约束**：CPT需要在64 GPU上训练10B tokens，1.7B模型可以纯数据并行（TP=1, PP=1, DP=64），训练效率最高
2. **验证目的**：CPT项目的核心目标是验证地理垂域数据的有效性和编码方案的可行性，1.7B模型足以验证这些假设
3. **迭代速度**：小模型训练快，可以快速迭代数据配比、编码方案等超参数
4. **成本考虑**：更大的模型（如7B/14B）在64卡上需要引入TP/PP并行，通信开销增大，且训练时间成倍增加

**关于MOE架构：**
- CPT阶段没有评估MOE，因为CPT的目标是基座能力注入，MOE更适合后训练阶段
- 在STAgent项目中，我们最终使用的是Qwen3-30B-A3B（MoE架构，激活3B参数），验证了MOE在Agentic场景的有效性——参数效率提升200倍以上（30B MoE激活3B vs 671B Dense）

---

### Q: 模型在线上运行时，是纯生成式 Base Model，还是与工具调用系统深度集成？

**回答：**
是**与工具调用系统深度集成**的，具体调用逻辑如下：

```
用户Query → LLM生成Thought+Action → MCPClient发送JSON-RPC请求 
→ MCP Server执行工具 → 返回Observation → LLM继续生成 → ... → 最终Answer
```

**架构细节：**
1. **FastMCP协议**：基于HTTP/SSE长连接 + JSON-RPC通信，工具运行在独立的Server进程中，与LLM推理完全解耦
2. **10类工具**：get_current_time、get_navigation、get_poi_info、get_location、get_adcode、search_around_poi、search_poi_along_route、search_flights、search_train_tickets、universal_search
3. **Context机制**：工具返回结果存储在ContextManager中，LLM通过ID引用获取数据，避免上下文膨胀
4. **双Server架构**：端口40003（缓存Server，LRU Cache maxsize=128）用于RL训练，端口40001（实时Server，无缓存）用于线上/SFT

模型不是纯生成式的，它需要在推理过程中多次与工具Server交互，是一个典型的Agentic系统。

---

### Q: 百度实习期间刷单识别项目中，基于大模型 SFT 的召回率为何只有 94%？漏掉的 6% 样本具有什么特征？

**回答：**
94%的召回率主要受限于以下因素：

漏掉的6%样本主要具有以下特征：
1. **高度伪装的刷单行为**：这些样本的对话模式与正常用户高度相似，刷单者使用了更自然的语言表达，没有明显的模板化特征
2. **新型刷单模式**：训练数据中未覆盖的新型刷单手法，模型没有见过类似的pattern
3. **边界样本**：处于正常交易和刷单之间的灰色地带，即使人工标注也存在分歧

这个项目让我认识到，大模型SFT在风控场景中的局限性——对于对抗性强的场景，纯SFT的泛化能力有限，需要结合规则引擎和实时特征进行多层防御。

---

### Q: 在高德半年实习期间，最大的收获和对个人成长影响最深的部分？

**回答：**
最大的收获是**从"调参工程师"成长为"问题解决者"**。

具体来说：
1. **学会了从问题本质出发**：RL训练不稳定时，我最初的反应是调KL系数、调学习率，但这些都是在"症状"上打补丁。直到读了RAGEN论文才理解Echo Trap的根因是样本选择问题，才设计出DA-GRPO。这教会我不要盲目调参，要先理解问题本质。

2. **学术论文是解决工程问题的武器**：DA-GRPO的核心思想来自RAGEN的方差过滤和SimpleTIR的Void Turn分析。在工业界遇到的问题，往往能在学术界找到理论支撑。

3. **小规模验证再上全量**：DA-GRPO的每个设计决策（λ值、方差过滤比例、W_sft计算方式）都先在小数据+少步数上验证，确认有效后才在96 GPU上全量训练，节省了大量资源。

4. **端到端的项目经验**：从数据采集、清洗、标注，到SFT训练、RL训练、评测，完整经历了大模型训练的全流程。

---

### Q: 实验室算力资源是否限制了训练效率？在高德使用 ROLL 框架训练时，是否对速度瓶颈做过分析或优化？

**回答：**

**算力资源方面：**
- 实验室算力有限，主要用于小规模实验验证
- 高德提供了Nebula集群（H200/H20 GPU），RL训练使用96 GPU（12 workers × 8 GPU），CPT训练使用64 GPU

**速度瓶颈分析与优化：**

1. **Rollout是最大瓶颈**：Agentic RL的rollout需要多轮MCP工具调用（网络I/O），128 batch × 16 rollout = 2048条轨迹，每条都需要多轮交互
   - 优化：LRU缓存（同一query的16个rollout调用相同工具时，只有第一次真正调用API）
   - 优化：SGLang async mode + RadixAttention（复用共享prefix的KV cache）
   - 优化：`async_generation_ratio=4`异步生成

2. **GPU显存争抢**：Actor训练、Rollout推理、Ref模型三者共享GPU
   - 优化：调整`gpu_memory_utilization`（本地0.7，集群0.8）
   - 优化：gradient_checkpointing + ref param_offload（本地训练时ref卸载到CPU）

3. **长尾等待**：异步rollout中最慢的轨迹决定整个batch的完成时间
   - 优化：设置`max_actions_per_traj=15`和`max_tokens_per_step=4096`限制轨迹长度

4. **H200 vs H20对比**：H200吞吐量约为H20的4倍，显存48GB vs 96GB但计算效率更高

---

### Q: 模型训练中最常用的 loss 是哪一种？baseline loss 的原始计算方式？

**回答：**

**最常用的loss：**
- **SFT阶段**：标准的**Cross-Entropy Loss**（交叉熵损失），这是最基础也是最常用的
- **RL阶段**：**GRPO Loss**（Group Relative Policy Optimization Loss）

**Cross-Entropy Loss的计算方式：**
```
L = -1/N × Σ log P(y_t | y_{<t}, x)
```
- 对于每个位置t，计算模型预测的概率分布与真实token之间的交叉熵
- 只在labels不为-100的位置计算（即只在assistant response部分计算）
- N是有效token的数量

**GRPO Loss的计算方式：**
```
A_i = (R_i - mean(R)) / std(R)    # 组内标准化作为Advantage
L = -min(ratio × A, clip(ratio, 1±ε) × A)    # Clip约束的策略梯度
```
- 对同一prompt的K个response，用组内reward的均值和标准差进行标准化
- ratio = π_θ(a|s) / π_ref(a|s)，当前策略与参考策略的概率比
- 加上KL散度约束防止策略偏离过大

**其他使用过的loss：**
- **IFT Loss**（Implicit Future Training）：在SFT框架中实现，缓解exposure bias
- **KD Loss**（知识蒸馏）：`L = (1-α) × CE_loss + α × KL_divergence(student, teacher)`

---

### Q: 日常主要使用哪些 AI 编程辅助工具？

**回答：**
- **Claude Code**：主要用于项目文档整理、代码理解、技术方案分析
- **Cursor**：日常编码时使用，代码补全和重构
- **GitHub Copilot**：之前使用过，后来切换到Cursor

没有使用过阿里云相关的AI Coding工具（如通义灵码），但了解其定位和功能。

---

### Q: 你所在团队当前的技术方向和主要工作内容？

**回答：**
我所在的高德AI团队主要工作内容包括：

1. **STAgent（时空规划Agent）**：让高德智能助手能够理解用户出行意图，自主调用地图工具完成规划。这是团队的核心项目。

2. **地理信息基座模型（CPT）**：通过大规模地理垂域数据的持续预训练，增强LLM对地理空间的理解能力。基于Qwen3-1.7B，使用Megatron-LM在64 GPU上训练。

3. **评测体系建设**：构建面向高德业务场景的评测框架，覆盖意图理解、地理知识问答、POI推荐等任务。

团队仍在开展Base Model训练（CPT项目），同时重点推进Agent能力的RL优化。技术方向上，团队关注Agentic AI、地理信息理解、多模态（结合地图图像和文本）等方向。

---

---

### Q: 项目迭代中，是否频繁出现因 Prompt 设计或模型行为偏差导致返工的问题？如何系统性解决？

**回答：**
确实频繁出现过，这是Agentic RL项目的常态。我们经历了11轮SFT迭代和11+轮RL版本迭代，其中多次因为Prompt设计或模型行为偏差导致返工。

**典型返工案例：**
1. **RL V4**：负tool reward设置过于极端，模型学会了"不调用工具"来避免惩罚，整轮训练作废
2. **RL V3**：换成30B-A3B模型后OOM，需要重新调整并行策略（从tp=pp=ep=cp=2改为tp=cp=2, pp=ep=4）
3. **RL V5**：工具连接timeout，训练卡住，需要排查MCP Server的连接池问题
4. **SFT数据迭代**：早期teacher模型生成的轨迹幻觉率高，导致SFT后模型也学会了"编造"POI，需要重新清洗数据

**系统性解决方案：**

1. **小规模验证机制**：每次大改（reward设计、模型切换、数据替换）前，先在小数据（1K query）+ 少步数（50步）上验证，确认方向正确后才上全量。这个机制在后期节省了大量GPU资源。

2. **监控看板**：使用SwanLab实时监控训练指标（reward均值/标准差、工具调用率、Echo Trap发生率、梯度范数），一旦发现异常指标（如reward标准差骤降）立即停止训练排查。

3. **版本化管理**：每轮SFT和RL的checkpoint、配置文件、数据版本都严格记录（见ROLL-Agentic RL.md中的版本迭代表），方便回溯和对比。

4. **渐进式迭代**：不做大跨度的改动，每次只改一个变量（如只改reward设计，或只换模型），便于定位问题原因。

5. **DA-GRPO的自适应机制**：从算法层面解决了部分返工问题——方差过滤自动跳过无效样本，difficulty-aware加权自动聚焦有价值的样本，减少了因数据质量问题导致的训练失败。

---

### 提问环节

> 注：以下是面试中"你有什么想问的"环节，我向面试官提问后，面试官的追问及我的回答。

#### Q: 团队对外提供的技术能力定位，以及与通义千问等大模型之间的协作关系？

**回答：**
据我了解，PAI（Platform for AI）是阿里云的AI基础设施平台，核心定位是为内外部客户提供**模型训练、推理部署、数据处理**的全链路AI工程能力。与通义千问的关系是"平台+模型"的协作模式——通义千问团队负责模型研发和算法创新，PAI团队负责提供高效的训练基础设施（如分布式训练框架、GPU集群调度）和推理部署能力（如模型压缩、推理加速）。

从我在高德的实习经验来看，我们使用的ROLL框架就是阿里内部的RL训练基础设施，它支持多种训练后端（Megatron/DeepSpeed）和推理引擎（vLLM/SGLang），这类框架的开发和维护可能就是PAI团队的核心工作之一。我对这种"让算法团队专注于算法创新，平台团队负责工程效率"的分工模式非常感兴趣。

#### Q: 团队在 RL 训练优化中重点关注哪些问题？

**回答：**
基于我在ROLL框架上的使用经验，我认为RL训练优化中有几个关键问题：

1. **GPU利用率**：RL训练中推理和训练交替执行，GPU存在大量空闲时间。ROLL通过Colocated部署的offload/reload机制实现时分复用，但仍有优化空间（如更细粒度的流水线化）。

2. **Rollout效率**：特别是Agentic场景，多轮工具交互导致rollout极慢。异步生成、KV cache复用（SGLang RadixAttention）、工具调用缓存等都是重要的优化方向。

3. **大规模分布式训练的稳定性**：多节点训练中的NCCL通信故障、GPU故障容错、checkpoint断点续训等工程问题。

4. **训练-推理后端的统一调度**：如何在Megatron训练和vLLM/SGLang推理之间高效地进行模型参数同步，减少数据搬运开销。

在与客户的分工上，我理解PAI团队更侧重**速度优化和系统稳定性**（框架层面），客户（如高德AI团队）更侧重**效果优化**（算法和数据层面）。两者通过YAML配置接口解耦。

#### Q: AI Infra 优化工作属于哪个技术层级？

**回答：**
AI Infra优化通常可以分为三个技术层级：

1. **应用层**：训练框架（如ROLL、Megatron-LM）、RL算法实现（GRPO/PPO）、数据Pipeline优化。这是与算法最接近的层级。

2. **系统层**：分布式调度（Ray集群管理）、通信优化（NCCL AllReduce/ReduceScatter）、显存管理（ZeRO分片、offload策略）、推理引擎优化（PagedAttention、RadixAttention）。这是AI Infra的核心层级。

3. **硬件层**：GPU驱动优化、CUDA kernel编写、硬件拓扑感知的通信优化。这通常由NVIDIA或专门的硬件团队负责。

从我的经验来看，我在高德实习期间的工作主要在**应用层**（使用ROLL框架进行RL训练、调优训练参数）和部分**系统层**（GPU显存分配优化、异步Rollout资源调度、多节点通信问题排查）。如果加入PAI团队，我希望能更深入地参与**系统层**的工作，特别是分布式训练框架的设计和推理引擎的优化，这与我在ROLL框架上积累的实践经验高度相关。
